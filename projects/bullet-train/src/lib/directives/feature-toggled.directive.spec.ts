import { FeatureToggledDirective } from './feature-toggled.directive';

describe('FeatureToggledDirective', () => {
  it('should create an instance', () => {
    const directive = new FeatureToggledDirective();
    expect(directive).toBeTruthy();
  });
});
